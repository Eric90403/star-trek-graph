#!/bin/bash
# Talk to Picard (alias for: ./trek --character PICARD)
DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "$DIR/.venv/bin/python" "$DIR/src/character_agent.py" --character PICARD "$@"
